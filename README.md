# TP Jour 4 — Pipeline Jenkins/Ansible

Déploiement automatique d'un site nginx via pipeline Jenkins → Ansible.

## Lancement local
\`\`\`bash
cd ansible
ansible-playbook -i inventory.ini site.yml
\`\`\`
